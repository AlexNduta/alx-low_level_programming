#include "main.h"
/**
 * _isupper - checks if the provided letter is uppercase
*@c: charachter to check
*
*Return: 0 or 1
 */
int _isupper(int c)
{
	if (isupper(c))
		return (1);
else
		return (0);
}
