#include <stdio.h>
/**
 * main - A program that prints text
 * Return 0 (success)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
